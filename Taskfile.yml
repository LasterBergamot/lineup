version: "3"

tasks:
  run:
    desc: Run the application
    cmd: uv run python main.py

  install:
    desc: Install dependencies
    cmd: uv sync

  lint:
    desc: Lint the codebase
    cmd: uv run ruff check .

  format:
    desc: Format the codebase
    cmd: uv run ruff format .

  serve:
    desc: Start the API server
    cmd: uv run uvicorn app:app --reload

  test:
    desc: Run tests with coverage (>=80%)
    cmd: uv run pytest --cov=lineup --cov-report=term-missing

  build:
    desc: Build the container image
    cmd: podman build -t lineup .

  rebuild:
    desc: Force a fresh container image build (no cache)
    cmd: podman build --no-cache -t lineup .

  up:
    desc: Start the API in a container (detached)
    cmd: podman compose up -d

  down:
    desc: Stop the container
    cmd: podman compose down
